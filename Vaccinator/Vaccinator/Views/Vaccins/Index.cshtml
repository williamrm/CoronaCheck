@model IEnumerable<MvcVaccin.Models.Vaccin>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.marque)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.num_lot)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.marque)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.num_lot)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idVaccin">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idVaccin">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idVaccin">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
